export enum StockStatus {
  IN_STOCK = "in stock",
  OUT_STOCK = "out of stock",
  LOW_STOCK = "low on stock",
}

export enum ProductSize {
  SMALL = "Small",
  STANDARD = "Standard",
  LARGE = "Large",
}
